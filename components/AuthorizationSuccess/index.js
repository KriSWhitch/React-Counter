import React from "react";
import DisplayReduxState from "../DisplayReduxState/index";

const AuthorizationSuccess = (props) => (
  <div>
    <DisplayReduxState {...props} />
  </div>
);

export default AuthorizationSuccess;
